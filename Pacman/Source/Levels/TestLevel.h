#pragma once

#include "BaseLevel.h"

void TestLevel_Init(LevelManager* data);
void TestLevel_Update(float deltaTime, LevelManager* data);
void TestLevel_Render(float deltaTime, LevelManager* data);
void TestLevel_Destroy(LevelManager* data);